interface:
  display_name: "Observability Designer"
  short_description: "Design actionable service telemetry and alerts."
  default_prompt: "Use $observability-designer to map user outcomes and failure modes to SLIs, telemetry, alerts, and runbooks."
policy:
  allow_implicit_invocation: true
